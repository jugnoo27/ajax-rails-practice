<% if @user.errors.any? %>
  $('#errorArea').html("<%= escape_javascript render('errors', user: @user) %>")
<% else %>
  $('#usersTable').prepend("<%= escape_javascript render('user_row', user: @user)%>");
  $('.close-modal').click();
<% end %>
